<UserControl x:Class="SLStudio.RpkEditor.Modules.RpkEditor.Views.RpkDesignerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:properties="clr-namespace:SLStudio.RpkEditor.Modules.RpkEditor.Resources"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:SLStudio.RpkEditor.Converters"
             xmlns:gong="urn:gong-wpf-dragdrop"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <!--Content-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.InputBindings>
            <KeyBinding Key="S" Modifiers="Alt" Command="{DXCommand '@e(searchBox).Focus()'}" />
        </Grid.InputBindings>

        <!--Search-->
        <TextBox x:Name="searchBox" Grid.Row="0" BorderThickness="0"
                 metro:TextBoxHelper.Watermark="{x:Static properties:RpkEditorResources.SearchAltS}"
                 Style="{StaticResource StudioSearchTextBoxStyle}" />

        <!--Items-->
        <ListBox Grid.Row="1" Margin="{StaticResource SmallUpMargin}"
                 ItemsSource="{Binding Resources}"
                 SelectionMode="Extended" BorderThickness="0"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 AlternationCount="{Binding RelativeSource={RelativeSource Self}, Path=Items.Count}"
                 gong:DragDrop.IsDropTarget="True"
                 gong:DragDrop.IsDragSource="True"
                 gong:DragDrop.DropHandler="{Binding}"
                 gong:DragDrop.UseDefaultDragAdorner="True">
            <!--Resources-->
            <ListBox.Resources>
                <Style TargetType="{x:Type ScrollViewer}" BasedOn="{StaticResource {x:Type ScrollViewer}}">
                    <Setter Property="OverridesDefaultStyle" Value="True" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <Border Grid.Column="0" BorderBrush="{StaticResource Border}">
                                        <Grid>
                                            <!--HasError-->
                                            <Rectangle Width="20" HorizontalAlignment="Left"
                                                       Fill="{StaticResource Border}" />

                                            <!--LineNumber-->
                                            <Rectangle Margin="20, 0, 0, 0" Width="50"  HorizontalAlignment="Left" />

                                            <!--Separator-->
                                            <Rectangle Margin="70, 0, 0, 0" Width="1" HorizontalAlignment="Left"
                                                       Fill="{StaticResource Focused}" />

                                            <!--Content-->
                                            <ScrollContentPresenter />
                                        </Grid>
                                    </Border>

                                    <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Column="1"
                                               Value="{TemplateBinding VerticalOffset}"
                                               Maximum="{TemplateBinding ScrollableHeight}"
                                               ViewportSize="{TemplateBinding ViewportHeight}"
                                               Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />

                                    <ScrollBar x:Name="PART_HorizontalScrollBar"
                                               Orientation="Horizontal"
                                               Grid.Row="1" Grid.Column="0"
                                               Value="{TemplateBinding HorizontalOffset}"
                                               Maximum="{TemplateBinding ScrollableWidth}"
                                               ViewportSize="{TemplateBinding ViewportWidth}"
                                               Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.Resources>

            <!--ItemTemplate-->
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Height="48">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal"
                                    Background="{StaticResource Editor.Background}">
                            <!--HasError-->
                            <Grid Width="20" Background="{StaticResource Border}">
                                <Image Margin="1" Source="{StaticResource TaskCenterStatusBarAni_00}" Visibility="Collapsed" />
                            </Grid>

                            <!--LineNumber-->
                            <TextBlock Margin="5, 0, 15, 0" Width="30" FontSize="14"
                                       FontFamily="Consolas" TextAlignment="Right"
                                       Foreground="{StaticResource IconBlueHighlight}"
                                       VerticalAlignment="Center" HorizontalAlignment="Center"
                                       Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TemplatedParent.(ItemsControl.AlternationIndex)}">
                                <TextBlock.Style>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="FontWeight" Value="Normal" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected,RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                                <Setter Property="FontWeight" Value="Bold" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <!--Separator-->
                            <Rectangle Width="1" Fill="{StaticResource Focused}" />
                        </StackPanel>

                        <!--Icon-->
                        <Image Grid.Column="1" Margin="6"
                               Source="{Binding IconSource, Converter={StaticResource ImageSourceConverter}}" />

                        <!--Name/Description-->
                        <Grid Grid.Column="2" Margin="{StaticResource LeftMargin}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!--Resources-->
                            <Grid.Resources>
                                <Style x:Key="HighlightTextblockStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
                                    <Setter Property="Foreground" Value="{StaticResource Focused}" />
                                    <Setter Property="FontStyle" Value="Italic" />
                                    <Setter Property="FontSize" Value="16" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                            <Setter Property="Foreground" Value="Black" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Resources>

                            <!--Name-->
                            <StackPanel Grid.Row="0" Orientation="Horizontal">
                                <!--DisplayName-->
                                <TextBlock Text="{Binding DisplayName, Mode=OneWay}" FontSize="16" />

                                <!--Separator-->
                                <TextBlock Text=" - " FontSize="16" />

                                <!--Alias-->
                                <TextBlock Text="{Binding Alias, Mode=OneWay}"
                                           Style="{StaticResource HighlightTextblockStyle}"
                                           metro:VisibilityHelper.IsVisible="{Binding Alias, Converter={StaticResource IsNotNullConverter}}" />

                                <!--TypeId-->
                                <TextBlock Text="{Binding TypeId, Converter={converters:IntToHexIdConverter}}"
                                           Style="{StaticResource HighlightTextblockStyle}"
                                           metro:VisibilityHelper.IsVisible="{Binding Alias, Converter={StaticResource IsNullConverter}}" />
                            </StackPanel>

                            <!--Description-->
                            <ItemsControl Grid.Row="1" ItemsSource="{Binding Description}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Margin="{StaticResource SmallRightMargin}"
                                                   Text="{Binding Text}">
                                            <TextBlock.Style>
                                                <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Highlight}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource Focused}" />
                                                            <Setter Property="FontStyle" Value="Italic" />
                                                        </DataTrigger>

                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding Highlight}" Value="True" />
                                                                <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True" />
                                                            </MultiDataTrigger.Conditions>

                                                            <Setter Property="Foreground" Value="Black" />
                                                        </MultiDataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>

            <!--ItemContainerStyle-->
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="Padding" Value="0" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</UserControl>