<metro:MetroWindow x:Class="SLStudio.Core.Modules.Console.Views.ConsoleView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:resx="clr-namespace:SLStudio.Core.Modules.Console.Resources"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   mc:Ignorable="d" Style="{StaticResource DefaultWindowStyle}"
                   MinWidth="970" MinHeight="512" Width="970" Height="512" 
                   WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Menu-->
        <StackPanel Orientation="Horizontal" Margin="{StaticResource SmallUpperMargin}">
            <!--Clear-->
            <Button x:Name="Clear" Margin="{StaticResource SmallLeftMargin}"
                    BorderThickness="0" Focusable="False"
                    ToolTip="{x:Static resx:Console.ClearAll}" >
                <Image Source="pack://application:,,,/Core/Resources/Images/VS2017/ClearWindowContent_16x.png"/>
            </Button>

            <!--Separator-->
            <Separator Margin="{StaticResource SmallLeftMargin}" Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"/>
            
            <!--TextWrapping-->
            <ToggleButton IsChecked="{Binding TextWrapping}" Margin="{StaticResource SmallLeftMargin}"
                          BorderThickness="0" Focusable="False"
                          ToolTip="{x:Static resx:Console.TextWrapping}">
                <Image Source="pack://application:,,,/Core/Resources/Images/VS2017/WordWrap_16x.png"/>
                <ToggleButton.Style>
                    <Style TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource MetroToggleButton}">
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter Property="Background" Value="Transparent" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentColorBrush}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ToggleButton.Style>
            </ToggleButton>
        </StackPanel>

        <!--Console-->
        <TextBox x:Name="Console" Grid.Row="1" Margin="{StaticResource SmallFullMargin}"
                 Text="{Binding Text}" IsReadOnly="True" IsReadOnlyCaretVisible="True"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 BorderThickness="0" TextChanged="ConsoleTextChanged">
            <TextBox.Style>
                <Style TargetType="{x:Type TextBox}">
                    <Setter Property="TextWrapping" Value="NoWrap"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding TextWrapping}" Value="True">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>
    </Grid>
</metro:MetroWindow>
