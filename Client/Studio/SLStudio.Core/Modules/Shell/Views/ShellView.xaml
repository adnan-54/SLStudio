<Window x:Class="SLStudio.Core.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:core="clr-namespace:SLStudio.Core"
        xmlns:slstudio="https://slstudio.app/client/2021/xaml/"
        mc:Ignorable="d" Height="450" Width="800"
        d:DataContext="{d:DesignInstance Type=core:ShellViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--MainMenu-->
        <slstudio:View Grid.Row="0" ViewModel="{Binding MainMenu}" />
        
        <!--ToolBar-->
        <slstudio:View Grid.Row="1" ViewModel="{Binding ToolBar}" />
        
        <!--Workspace-->
        <slstudio:View Grid.Row="2" ViewModel="{Binding Workspace}" />
        
        <!--StatusBar-->
        <slstudio:View Grid.Row="3" ViewModel="{Binding StatusBar}" />
    </Grid>
</Window>