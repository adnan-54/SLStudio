<slstudio:ModalWindow  x:Class="SLStudio.ShellView"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                        xmlns:slstudio="clr-namespace:SLStudio"
                        mc:Ignorable="d" 
                        d:DataContext="{d:DesignInstance Type=slstudio:ShellViewModel}"
                        Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--Menu-->
        <slstudio:View ViewModel="{Binding MainMenu}"/>

        <!--Toolbar-->

        <!--Docking-->

        <!--StatusBar-->
    </Grid>
</slstudio:ModalWindow>
